{
  "version": 2,

  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],

  "builds": [
    {
      "src": "/api/(.*)",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
